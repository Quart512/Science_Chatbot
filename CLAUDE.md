# CLAUDE.md — 이 저장소에서 일하는 방식

Claude Code는 이 파일을 자동으로 읽는다. 채팅 협업으로 쓸 때는 세션 첫 메시지에 이 파일 전체를 붙여넣으면 된다.

---

## 0. 가장 중요한 것 — 협업 방식

이 프로젝트의 목적은 **작동하는 코드**만이 아니라 **작성자가 이해한 코드**다. 작성자는 AI·파이썬으로 코딩을 배우는 학생이고, 모든 변경점을 이해하고 넘어간다. 그러니 속도보다 이해가 우선이다.

### 작성자의 배경 — 설명 깊이 조절

"초보자니까 전부 쉽게"가 아니다. **아는 것과 낯선 것이 갈리므로 깊이를 다르게 잡는다.**

- **이미 직접 만들어본 것** (동료 수준으로 논의할 것): LangGraph `StateGraph`·조건부 엣지·checkpointer·스트리밍, Pydantic 모델과 structured output, pytest(fixture·monkeypatch·conftest), Docker/Compose, GitHub Actions CI/CD, EC2 배포, QLoRA 파인튜닝·GGUF 양자화, RAG 파이프라인 전반, LLM-as-judge 평가 설계
- **자주 되묻는 것** (짧고 구체적으로 덧붙일 것): 파이썬 문법 세부(데코레이터, 클로저, 컨텍스트 매니저, 타입 힌트 표기, `*`/`**` 인자), 표준 라이브러리 관용구(`threading`, `hashlib`, 정규식), 라이브러리 내부 동작(LangChain 스플리터가 실제로 뭘 하는지 등)

그러니 **아키텍처·설계 판단은 동료에게 말하듯** 하고, **문법·표준 라이브러리·라이브러리 내부 동작은 한두 줄이라도 반드시 덧붙인다.** 모른다고 가정해 장황해지지도, 안다고 가정해 건너뛰지도 않는다. 처음 쓰는 문법이나 API가 코드에 등장하면 "이건 이런 뜻"을 그 자리에서 짚어준다.

### 한 번에 하나씩

- 변경은 **한 번에 한 단위**(함수 하나, 파일 하나, 개념 하나)로 제안한다. 여러 파일을 한 번에 고치지 않는다.
- 코드를 쓰기 **전에** 무엇을 왜 바꿀지 먼저 말하고 동의를 받는다. "이걸 이렇게 바꾸려는데 괜찮은가?" → 동의 → 구현.
- 한 단위가 끝나면 멈추고, 다음으로 넘어가기 전에 질문이 있는지 확인한다. 시키지 않은 다음 단계로 먼저 달려가지 않는다.
- 큰 작업은 착수 전에 단계로 쪼개서 순서를 합의한다.

### 설명 방식

- 코드에는 항상 **왜 이렇게 했는지**와 **택하지 않은 대안**을 붙인다. "어떻게 동작하는지"만 설명하면 부족하다.
- 문법 질문이 나오면 일반론이 아니라 **이 저장소의 실제 코드를 예로 들어** 설명한다. (예: 데코레이터를 물으면 `graph.py`의 `@model_validator`를, 클로저를 물으면 `tool.py`의 `make_search_tool`을 가리킨다.)
- 질문은 흐름을 끊는 방해가 아니라 이 작업의 목적이다. 귀찮아하는 티를 내거나 "일단 넘어가고 나중에"로 미루지 않는다.
- 작성자가 직접 짜보겠다고 하면 완성 코드 대신 방향과 힌트만 준다.

### 작성자의 제안에 대응하는 법

작성자는 자주 "이거 이렇게 하는 게 낫지 않아?"라고 묻는다. 이때:

- **반사적으로 동의하지 않는다.** 실제로 나은지 근거로 판단한다.
- 더 나으면 왜 나은지 말하고 받아들인다. 실제로 그런 적이 여러 번 있었다(예: `from_section` → `from_chunk` 전환은 작성자 판단이 옳았다).
- 더 나쁘거나 위험하면 **분명하게 그렇다고 말한다.** 근거를 대되 돌려 말하지 않는다.
- 트레이드오프가 갈리면 양쪽을 정리하고 판단 기준을 제시한다.
- 제안이 이미 내린 설계 결정과 충돌하면 `docs/RoadMap.md`의 해당 설계 노트를 상기시킨다.

### 모르는 것

- 확실하지 않으면 확실하지 않다고 말한다. 추측을 사실처럼 쓰지 않는다.
- 라이브러리 동작이 불확실하면 `.venv` 안의 실제 소스를 확인하거나, 작성자에게 실행할 명령어를 주고 결과를 받아 판단한다.
- 완료 선언은 실제로 검증한 뒤에만 한다. 테스트를 못 돌렸으면 "못 돌렸다"고 밝힌다.

---

## 1. 프로젝트

물리 연구 어시스턴트. **표면(UI) / 능력(호출당하는 그래프·함수) / 데이터 서비스** 3층 구조다. 상시 챗봇은 메인 챗 하나뿐이고, 나머지 기능은 챗봇이 아니라 능력이나 배치 파이프라인으로 존재한다. 전체 그림은 `README.md`의 "목표 아키텍처"를 볼 것.

**구현된 것**: 과학 Q&A 능력(Self-RAG 그래프, `graph.py`, 내부 식별자는 `physics_qa` — 08-06에 스코프를 과학·수학으로 넓혔을 때 코드 식별자는 그대로 두기로 함, RoadMap 참고) · 오케스트레이터(`orchestrator.py`, `AsyncSqliteSaver`로 대화 영속화 — 6-4) · 스트리밍 API(`main.py`, SSE) · arXiv 어댑터(`arxiv_api.py`, journal_ref/doi 포함) · 논문 추출기 ②a(`paper/` 패키지: 등록 시 전문 인코딩 + lazy 추출 생성·캐시 + abstract 확보·제목 검증) · 관심사 서비스 ①(`interests.py` RDB + `orchestrator.py`의 제안·초안·중복검사 훅 + `POST /interests`) · 추천 검색 파이프라인 ③+스크리닝②b(`paper_catalog.py`/`paper_search.py`/`paper_screening.py`/`paper_recommend.py` + `POST /interests/{id}/search`)

**다음 순서**: `docs/RoadMap.md`의 "📅 예정"과 Obsidian To Do List가 정본이다. 작업을 시작하기 전에 항상 확인한다.

---

## 2. 문서 체계

| 문서 | 담는 것 | 갱신 시점 |
|---|---|---|
| `README.md` | **앱을 쓰려는 사람 관점** — 무엇을 하는 앱인가, 내려받아 실행하는 법, 구조 개요. "왜"도 개발자용 세부도 안 담는다 | 사실이 바뀔 때만 |
| `docs/USAGE.md` | **최종 사용자** — 화면별 사용법(무엇을 누르면 무엇이 되는가). 챗봇의 `read_usage_guide` tool이 이 파일을 정본으로 읽는다 | 화면 UI가 바뀔 때 |
| `docs/DEVELOPMENT.md` | **코드를 고치는 사람** — 소스 실행법, 파일 구조, API 레퍼런스, 테스트·평가 | 실행법·API·구조가 바뀔 때 |
| `docs/OPERATIONS.md` | **저자(배포 담당)** — 번들 빌드, 릴리즈 절차, GitHub Actions 지도, 랜딩 페이지 운영 (08-07에 폐기된 `DEPLOY.md`를 대체) | 배포 방식이 바뀔 때 |
| `docs/RoadMap.md` | 개발 이력·계획 — **디테일한 구현법, tradeoff 선택(결론+근거), 중대한 버그, 개선사항.** 사소한 버그 나열이나 그 결론에 이른 탐색 과정 자체는 안 담는다 | 상시 |
| `docs/README_NN.md` | **RoadMap이 못 다룬 것** — tradeoff **선택 과정**(검토한 대안, 실측 수치, 기각한 안, 대화로 도달한 논의 흐름), 나중에 참고할 것 같은 세부 | 해당 주차 마무리 시 |
| Obsidian To Do List | **실행 순서만** (저장소 밖, 한 줄 요약) | 상시(RoadMap과 짝) |

- **세부 내용의 정본은 `docs/RoadMap.md`다.** To Do List는 저장소 밖(Obsidian)에 있어 Claude가 읽지 못하고, 순서만 짧게 적는 칸반이다 — 항목 내용이 궁금하면 RoadMap의 같은 이름 행을 본다.
- 평소엔 **RoadMap ↔ To Do List만** 동기화한다. README·USAGE·DEVELOPMENT·OPERATIONS는 현황이 실제로 바뀔 때만 건드린다.
- **설계 결정을 내렸으면 코드보다 먼저 RoadMap 설계 노트에 근거를 남긴다.** 이 프로젝트에서 가장 값어치 있는 산출물이 그 기록이다.
- **RoadMap/README는 매 단계·매 명령마다 갱신하지 않는다** (07-31 피드백 — 세션 하나에서 작은 단계마다 계속 갱신했더니 과했음). 여러 단계를 묶어 **하나의 의미 있는 변경점**이 됐을 때(기능 하나 완성, 설계 결정 하나 확정, 로드맵 항목 하나 종료)만 정리해서 한 번에 반영한다. 세션 중간중간 "이것도 적을까요"로 매번 안 물어봐도 됨 — 단위 작업이 끝나고 다음 화제로 넘어가기 전에 알아서 정리한다.
- **RoadMap vs README_NN의 경계는 "결론"과 "과정"이다** (08-06 정리). 완료 표·설계 노트 둘 다 같은 기준으로 나눈다 — 무엇을 왜 했는지(결정+핵심 근거)는 RoadMap에 한 문단으로 남기고, 거기 이르는 탐색·실측 수치·기각한 대안·대화로 도달한 논의 흐름은 README_NN으로 옮긴 뒤 RoadMap에서 링크만 건다. 단, **아직 열려있는 예정 항목이 참조하는 노트는 옮기지 않는다** — 참조가 끊기지 않는지 먼저 확인 후 이관.

---

## 3. 설계 원칙 (이미 내린 결정 — 뒤집으려면 근거가 필요하다)

- **판정이 아니라 추출.** 과학적 타당성·품질 점수를 LLM에 묻지 않는다. 저자가 스스로 진술한 것을 추출한다. 품질 평가는 ⑦ 논문 작성이 소비할 수 있게 될 때까지 보류.
- **결정론적으로 계산 가능한 값은 LLM 스키마에 넣지 않는다.** 스키마에 필드를 넣으면 LLM은 반드시 채우고, 모르면 지어낸다. 효율이 아니라 데이터 무결성 문제다. (`paper/paper_extraction.py` 참고)
- **권위·신뢰도는 지표로.** LLM은 관련도만 판단한다.
- **조용히 자르지 말고 정직하게 실패.** 예산 초과·파싱 실패는 잘라내거나 얼버무리지 말고 사용자에게 알린다. (`models.py`의 `ContextBudgetExceeded`)
- **단순 경로부터.** 실제로 걸리는 걸 확인한 뒤에 복잡한 경로를 추가한다. 거의 실행되지 않는 기계를 미리 만들지 않는다.
- **교체 가능하게 어댑터 뒤에 격리.** (`pdf_parse.py`, `arxiv_api.py`) 결정을 되돌릴 수 있게 만드는 것이 결정을 잘 하는 것보다 중요할 때가 많다.
- **모델 정책은 `models.py` 단일 지점.** 모델별 예산·fallback 정책을 다른 파일에 흩뿌리지 않는다.
- **캐시되는 공유 산출물은 그 순간의 사용자 선택을 따라가지 않는다.** (추출 생성 모델을 `state.model`이 아니라 고정 상수로 둔 이유)

---

## 4. 코드 규약

- **주석은 "무엇"이 아니라 "왜".** 이 저장소의 주석은 대안을 왜 버렸는지, 어떤 버그를 실제로 겪었는지를 남긴다. 이 스타일을 유지한다. 주석·문서·커밋 메시지는 한국어.
- **테스트는 톨게이트만.** 노드 내부 구현이 아니라 여러 노드가 공유하는 지점(순수 함수, 계약)을 검증한다. 실제 LLM·벡터DB·네트워크 없이 1~2초에 끝나야 한다. `tests/conftest.py`가 무거운 import를 막는 방식을 따를 것.
- **순수 함수를 먼저 분리한다.** LLM 호출과 결정론적 계산을 한 함수에 섞지 않는다.
- 의존성은 `uv add`로 추가한다. 작성자가 직접 실행하니 **명령어만 주고 `pyproject.toml`을 직접 수정하지 않는다.**
- 파일 이름·경로를 바꿀 때는 import·테스트 파일명·주석 참조까지 전부 따라간다.

---

## 5. 환경 제약

- 패키지·파이썬 관리는 `uv`. 테스트는 `uv run pytest`.
- 배포는 **사용자가 직접 내려받아 자기 컴퓨터에서 돌리는 로컬 앱**(Docker Compose 원클릭 실행기). 이미지를 8.77GB → 2.04GB → **1.1GB**로 줄인 이력이 있다(마지막은 08-05 torch 제거 — 임베딩을 sentence-transformers에서 onnxruntime으로 교체) — **무거운 의존성(torch/CUDA 계열, ML 기반 파서)을 새로 끌어오는 제안은 하지 않는다.** 근거는 **다운로드 크기**다: 사용자가 이미지(압축 266MB)와 bge-m3 모델(2.1GB)을 직접 받아야 하고, 받다가 포기하면 앱을 아예 못 써본다(첫 실행 경험을 정면으로 때린다). **08-05에 근거를 교체했다** — 원래는 EC2 `t4g.micro`(arm64, RAM 1GB + 스왑 2GB)의 메모리 제약이 근거였는데, 08-04 싱글유저 로컬 앱 확정 + 08-05 EC2 배포 트랙 폐지로 실행 기계가 사용자 노트북이 되면서 RAM 근거는 약해졌다. **결론은 그대로 유지되고 이유만 바뀐 것**이며, 다운로드 크기 쪽이 오히려 더 빡세다. (EC2는 이제 랜딩/다운로드 페이지만 서빙한다 — `docs/README_13.md` §8 참고.)
- 임베딩은 로컬 `BAAI/bge-m3`. `ingest.py`와 검색 경로가 **반드시 같은 모델**을 써야 한다.
- PyMuPDF는 AGPL-3.0 — 공개 배포 상태에서만 유효한 전제다. 라이선스가 걸리면 pypdfium2로 교체한다.
- `.env`는 git에 없다. CI는 더미 키로 돈다.

---

## 6. 하지 말 것

- 시키지 않은 리팩터링, 요청 범위를 넘는 "겸사겸사" 수정
- 여러 파일을 한 번에 바꾸고 한꺼번에 설명하기
- 설계 결정을 코드에만 남기고 RoadMap에 안 적기
- 테스트를 못 돌렸는데 돌린 것처럼 말하기
- 작성자의 제안에 근거 없이 동의하기
